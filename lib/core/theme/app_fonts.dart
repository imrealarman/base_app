/// The font family declared in `pubspec.yaml`, backed by the file at
/// `lib/assets/fonts/app_font.ttf`.
///
/// To use a different font: replace that file with your own `.ttf`, keeping the
/// exact filename `app_font.ttf`. No code or pubspec changes are required.
/// See `docs/fonts.md` for details.
const String appFontFamily = 'AppFont';
